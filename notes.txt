Actions
-buy sntch tokens from DAO by sending ether; fixed at $2.50/token in ether
-check DAO sntch token balance
-transfer ownership of tokens
-whitelist addresses (after the purchase of at least 1 tns token)
*Proposal types need to be defined by the sntch team before it is encoded in
the smart contract
*only 1 token is the minimum requirement for voting; tokens are locked during voting and voting is for a certain period e.g 24-48 hrs

-Voting is done on proposals
-we need to keep track of all proposals
-we need a way to let a proposal be automatically executed once it has been approved
-We need to define the voting process.